.NET Core  Web.API (C#) that should generate a random user account and return it in JSON format.
Request:

/api/user/random (GET)
Sample Response:
{"name": "John Doe", "id": 3242324, "url":"link to random JPG image" }
